<div class="page-header">
    <h2>Nova senha</h2>
    <i>Entre com uma nova senha para o seu perfil!</i>
</div>

<section>

    <form class="form-signin" action="<?= $sessions_new_password_path(); ?>" method="POST">

        <?=text_field( new Field('password'), array('name' => 'password', 'title'=> 'Senha', 'no_array_name' => true,
            'type' => 'password') ); ?>
        <?=text_field( new Field('password_confirmation'),
            array('name' => 'password_confirmation', 'title'=> 'confirme a senha', 'no_array_name' => true,
                'type' => 'password') ); ?>

        <button class="btn btn-lg btn-primary" type="submit">Confirmar</button>

    </form>

</section>
